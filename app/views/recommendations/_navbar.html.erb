<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container d-flex align-items-center justify-content-between">
    <div class="d-flex align-items-center">
      <a class="navbar-brand mb-0 h1" href="#">Pagepal</a>

      <% if user_signed_in? %>
        <% if current_user.profile_image.attached? %>
          <%= image_tag current_user.profile_image.variant(resize_to_fill: [40, 40]), 
                alt: "Profile", 
                class: "rounded-circle", 
                style: "width:40px; height:40px; object-fit:cover;" %>
        <% else %>
        <%= image_tag "https://tse2.mm.bing.net/th?id=OIP.MNYMRopweKA9axhd73z_GwHaE8&pid=Api&P=0&h=180",
        alt: "Default Profile",
        class: "rounded-circle",
        style: "width:40px; height:40px; object-fit:cover;" %>
        <% end %>
      <% end %>
    </div>

    <% if user_signed_in? %>
      <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: "btn btn-outline-light" %>
    <% else %>
      <%= link_to 'Login', new_user_session_path, class: "btn btn-outline-light" %>
    <% end %>
  </div>
</nav>
